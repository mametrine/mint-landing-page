import { Layout } from "../components/Layout";

export const Home = () => (
  <div className="max-w-full bg-fixed bg-top overflow-hidden bg-[url('images/colourised-clouds.jpg')]">
    <Layout>
      Layout container
    </Layout>
  </div>
);

// url() is relative to the stylesheet.
